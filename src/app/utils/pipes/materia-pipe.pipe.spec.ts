import { MateriaPipePipe } from './materia-pipe.pipe';

describe('MateriaPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MateriaPipePipe();
    expect(pipe).toBeTruthy();
  });
});
